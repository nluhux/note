#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <errno.h>
#include <string.h>

int main(int argc, char *argv[]) {
	FILE *src = NULL;
	FILE *dest = NULL;

	if ((src = fopen(argv[1],"r")) == NULL) {
		perror("fopen() failed: ");
		exit(1);
	}
}
